GIT:
Inicializa un repositoro de git con un primer commit en la carpeta donde 
tengas los documentos del proyecto y crea una rama nueva con un par de commits

DOCKER:
usa la imagen nginx:stable-alpine3.23-perl

instala vim

crea una carpeta en /var/log/ que se llame carpeta_para_logs

copia el index.html en /usr/share/nginx/html/ 

documenta que vamos a usar el puerto 80

mediante el orquestador, crea el contenedor con el nombre servidor_examen

mapea el puerto de la maquina 8080 al 80 del contenedor

ejecuta el contenedor y visualizalo en el navegador

Cuando termines, junta la rama con la original y 

Sube el repositorio a github como público y entrega un documento con el enlace al repositorio en un documento:
"nombre_apellido_examen.txt"
